# Copy this file to .env.local and override as needed.
# .env.local is gitignored.

# Where the frontend talks to the backend. Must be reachable from the user's
# browser (i.e. NOT a docker-internal hostname).
NEXT_PUBLIC_API_URL=http://localhost:8000
